name: build

on:
  workflow_dispatch:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v4
    # local temurin to pin version @19 for gradle support
    - run: brew install --cask -v ./Formula/temurin.rb
    - run: brew install -v ./Formula/pylucene.rb
    - run: brew test pylucene
